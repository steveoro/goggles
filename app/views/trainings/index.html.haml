= render( partial: 'search_training_grid' )
= render( partial: 'goggles/feature_wip_disclaimer' )

%h3= @title

.row.col-xs-12
  = render_grid( @trainings_grid )

- if current_user && current_user.has_swimmer_confirmations?
  .row.col-xs-12
    &nbsp;
  .row.col-xs-12
    %a.btn.btn-primary(href="#{ new_training_path() }")
      = I18n.t('add_new')
