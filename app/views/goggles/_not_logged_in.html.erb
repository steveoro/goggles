<div class="hero-unit">
	<h3><%= t(:welcome) %></h3>
	<p>
		Work In Progress...
	</p>
	<p>
		<%= link_to( t('login'), new_user_session_path(), {:class => 'btn btn-primary btn-large'} ) %>
		or
		<%= link_to( t('sign_up'), new_user_registration_path(), {:class => 'btn btn-primary btn-large'} ) %>
	</p>
</div>
