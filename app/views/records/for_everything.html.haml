.row-fluid.span12#pageTop

#records_4x_grid
  .span12
    %h2
      = @title
      %span(id='loadingIndicator')
        = image_tag( 'indicator.gif' )

%script
  $.get( "#{ records_for_everything_path() }", function(data) {$( "#records_4x_grid" ).html( data );} );
