- cache( cache_key_for_meeting( 'show_ranking', @meeting.id, @max_mir_updated_at, @team ? @team.id: 0 ) ) do
  = render( partial: 'tabs_and_radio_title' )

  = render( partial: 'meeting_header' )

  .row-fluid
    .span12
      = render( partial: 'team_scores_grid' )
