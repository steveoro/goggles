.row-fluid#fin_score_calculation
  .goggles-category-gap#pageTop

.container-fluid
  .row-fluid.alert.alert-success.goggles-div-centeralign
    %h4
      = @tab_title
  .row-fluid
    = I18n.t('misc.how_to_calculate')
  .row-fluid
    %p/
  .row-fluid
    = form_tag( :misc_compute_fin_score, class: 'form-horizontal', remote: true, onsubmit: "$('#loadingIndicator').show(); $('#score_calculation_result').hide(); $('#fin_highlights').hide();" ) do
      = render( partial: 'calculation_parameters' )
      = render( partial: 'timing_parameters' )
      .control-group
        .controls
          = submit_tag( I18n.t('misc.compute'), id: "btn_submit", data: {disable_with: I18n.t(:please_wait), class: 'btn disabled'}, class: 'btn' )
          %span( id='loadingIndicator' hidden='true' )
            = image_tag( 'indicator.gif' )

#calculation_result
#fin_highlights
%p/
